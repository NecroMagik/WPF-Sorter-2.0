<Page
    x:Class="WPF_Sorter_2._0.Views.MainPage"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converters="clr-namespace:WPF_Sorter_2._0.Converters"
    xmlns:viewModels="clr-namespace:WPF_Sorter_2._0.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="600" 
    d:DesignWidth="900"
    d:DataContext="{d:DesignInstance viewModels:MainViewModel}">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanInverterConverter x:Key="BooleanInverterConverter" />
        <converters:ButtonTextConverter x:Key="ButtonTextConverter" />

        <Style x:Key="FolderCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray10}" />
            <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Gray8}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="Margin" Value="0,0,0,16" />
        </Style>

        <Style x:Key="SortButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="40,16" />
            <Setter Property="Height" Value="56" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border 
                            Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <ContentPresenter 
                                    VerticalAlignment="Center"
                                    Margin="0,0,8,0" />
                                <TextBlock 
                                    Text="{TemplateBinding Content}"
                                    VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock
                Grid.Row="0"
                Style="{StaticResource PageTitleStyle}"
                Text="Сортировка файлов"
                Margin="0,0,0,16" />

            <!-- Блок выбора папки -->
            <Border 
                Grid.Row="1"
                Style="{StaticResource FolderCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Text="📁 Выберите папку для сортировки"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Margin="0,0,0,12" />

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox
                            Grid.Column="0"
                            Text="{Binding SelectedFolder, UpdateSourceTrigger=PropertyChanged}"
                            Style="{DynamicResource MahApps.Styles.TextBox}"
                            FontSize="14"
                            VerticalContentAlignment="Center"
                            Height="36"
                            IsReadOnly="True"
                            ToolTip="Выберите папку" />

                        <Button
                            Grid.Column="1"
                            Content="Обзор"
                            Command="{Binding BrowseFolderCommand}"
                            Style="{DynamicResource MahApps.Styles.Button}"
                            Height="36"
                            Padding="20,0"
                            Margin="8,0,0,0"
                            FontWeight="SemiBold" />
                    </Grid>
                </Grid>
            </Border>

            <!-- Настройки поиска -->
            <Border 
                Grid.Row="2"
                Style="{StaticResource FolderCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <CheckBox
                        Grid.Column="0"
                        IsChecked="{Binding IncludeSubfolders}"
                        FontSize="14"
                        VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock
                                Text="📂"
                                FontSize="16"
                                Margin="0,0,8,0" />
                            <TextBlock
                                Text="Искать в подпапках"
                                VerticalAlignment="Center" />
                        </StackPanel>
                    </CheckBox>

                    <TextBlock
                        Grid.Column="2"
                        Text="{Binding FilesFoundText}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                        VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Кнопка сортировки -->
            <Button
    Grid.Row="3"
    Command="{Binding StartSortingCommand}"
    FontSize="18"
    FontWeight="SemiBold"
    Padding="40,16"
    Height="56"
    Cursor="Hand"
    Background="{DynamicResource MahApps.Brushes.Accent}"
    Foreground="White"
    BorderThickness="0"
    Margin="0,0,0,16"
    IsEnabled="{Binding IsSorting, Converter={StaticResource BooleanInverterConverter}}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border 
                Background="{TemplateBinding Background}"
                CornerRadius="8"
                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock 
                        Text="{TemplateBinding Content}"
                        VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Content" Value="🚀 Выполнить сортировку" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSorting}" Value="True">
                                <Setter Property="Content" Value="⏳ Сортировка выполняется..." />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectedFolder}" Value="">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Блок прогресса и статуса -->
            <Border
                Grid.Row="4"
                Background="{DynamicResource MahApps.Brushes.Gray10}"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,16"
                Visibility="{Binding ProgressVisibility}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Text="{Binding StatusText}"
                        FontSize="14"
                        Foreground="{DynamicResource MahApps.Brushes.Text}"
                        Margin="0,0,0,8" />

                    <ProgressBar
                        Grid.Row="1"
                        Value="{Binding ProgressValue}"
                        Minimum="0"
                        Maximum="100"
                        Height="20"
                        Foreground="{DynamicResource MahApps.Brushes.Accent}"
                        Margin="0,0,0,8" />

                    <TextBlock
                        Grid.Row="2"
                        Text="{Binding ProcessedFilesText}"
                        FontSize="12"
                        Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                        HorizontalAlignment="Right" />
                </Grid>
            </Border>

            <!-- Статусная строка (всегда видна) -->
            <TextBlock
                Grid.Row="5"
                Text="{Binding StatusText}"
                FontSize="13"
                Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                VerticalAlignment="Bottom"
                Margin="0,8,0,0" />
        </Grid>
    </ScrollViewer>
</Page>